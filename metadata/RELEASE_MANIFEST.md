# Release manifest

This staging candidate contains project-authored code, generated/derived result tables, calibration metadata, schedule hashes, and final figure artifacts. It does not contain raw restricted benchmark records or the obsolete historical monolithic pipeline.

The final M3 scalar-pricing statement is supported by `metadata/price_arm_schedule_equality.csv`: all 160 matched GermanOR/Mannino cells in each positive price arm match the zero-price schedule under both raw and canonical SHA-256 hashes.
