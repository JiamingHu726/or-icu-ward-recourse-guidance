name: or-icu-ward-recourse-py312
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.12.7
  - numpy=1.26.4
  - pandas=2.2.2
  - matplotlib=3.9.2
  - pyyaml=6.0.1
  - psutil=7.0.0
  - pip
  - pip:
      - gurobipy==13.0.1

# Verified top-level versions from metadata/runtime_manifest.json.
# Generate environment.lock.yml and metadata/pip_freeze.txt on the original
# experiment machine before the public Zenodo release.
