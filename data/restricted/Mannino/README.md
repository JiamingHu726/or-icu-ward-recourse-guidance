# Mannino restricted material

The Mannino raw workbook and derived duration pools are intentionally excluded from the public release. Use the preprocessing and generator code under `src/benchmark_calibration/`, together with `metadata/mannino_derived_pool_hash_manifest.csv`, to retrieve, regenerate, and verify the permitted artifacts from the provider-hosted resource.
