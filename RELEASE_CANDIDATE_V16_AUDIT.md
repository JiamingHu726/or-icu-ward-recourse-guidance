# v16 release-candidate audit

## Completed changes

- Adopted BSD-3-Clause for project-authored code and documentation.
- Replaced stale threshold framing with the final candidate-pool load-regime framing.
- Removed obsolete manuscript source and obsolete M1/M2/M3 publication artifacts.
- Added final M1/M2/M3 figures and dedicated final figure scripts.
- Added final seed-clustered tables, baseline-load validation, and fixed-schedule weight sensitivity outputs.
- Retained GermanOR frozen pools with required attribution; retained Mannino non-redistribution policy.
- Removed the historical monolithic publication pipeline runner rather than claiming unsupported end-to-end rerunnability.
- Removed cache artifacts and duplicated calibration archive.
- Updated source provenance to `duration_only` for GermanOR and Mannino cases.

## Intentional public-release placeholders

The repository owner, final creators, public URL, Zenodo DOI, version, and release date are intentionally unset. The staging check passes; the `--public` release check will remain blocked until those fields are completed.

## Recommended repository name

`or-icu-ward-recourse-guidance`
