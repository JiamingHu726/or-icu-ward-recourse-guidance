# Final figure scripts

- `make_M1_seed_clustered.py`: seed-clustered M1 load-response figure.
- `make_M2_seed_clustered_mechanism.py`: seed-clustered M2 component heatmaps and access-trade-off panel.
- `make_M3_attribution_validity.py`: zero-intervention and scalar-pricing schedule-identity validation.

Use only the final figures in the repository root `figures/`. Superseded threshold and stacked-median plotting code was removed.
